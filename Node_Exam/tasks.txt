4-5:
tcp соединение
на сервере надо в консоли написать ip адрес и порт клиента
и с помощью префикса echo сообщение от клиента 
на клиенте в терминале вводится сообщение которое отправляется серверу
соединение закрывается через минуту

4-5:
UDP. Сделайте сервер, который будет принимать сообщения от клиента, выводить их в консоль (с выводом с какого сокета отправлено) и отправлять обратно с префиксом "echo:". Сделайте клиента, который отправляет как сообщение текст, введённый через стандартный поток ввода.
TCP. Создайте сервер, который записывает полученные от клиента данные в файл с префиксом "Клиент начирикал: " (я не помню текста префикса). Создайте клиента, который каждые 10 секунд посылает текстовое сообщение вида [ДАННЫЕ УДАЛЕНЫ].

6-7:
MS SQL Server: начирикате http сервер, который по гет запросу с query параметром (id) будет возвращать json с паролем пользователя id. Пример записи в бд прилагается. Ошибки обрабатывать и возвращать как json

?:

первая задача

WebSocket создайте серве, клиет. клиент посылает серверу сообщение через поток, сервер
пересылает сообщение всем подключенным клиентам. через 20 секунд проискходит разрыв соеденения
клиент посылает своё имя через ввод с консоли, походу так к нему и надо обращаться.
само сообщение в коде
доебалась хуле не бродкаст, наверное это подразумевалось,но указано небыло

вторая

Http сервер, клиент посылает сообщение в котором есть имя и возраст через application/x-www-form-urlencoded.
Сервер принимает сообщение берет эту инфу записывает в json файл. обработать ошибки на сервере

4-5:

У меня задача была tcp сервер
На клиенте нужно отправлять каждын 10 секунд сообщение формата
Текст №n где текст вводится из консоли, а n-это номер сообщения
На сервере
В файл записывать смс формата "Message from ip клиента : port клиента: тут сообщенте от клиента"